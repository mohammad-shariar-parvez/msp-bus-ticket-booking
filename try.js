const res = { from: '', to: 'df', journeyDate: '2022-12-14' };

// do stuffar = [];
// console.log(res);

const tempFilters = {};
Object.keys(res).forEach((key) => {
	return key;
});
console.log(tempFilters);
console.log(res);